# PlanetGauge 0.1.5C 선별 재구성 계획 및 작업 회고

작성일: 2026-08-26  
목적: 다음 채팅에서 0.1.5A와 0.1.5B를 안전하게 보존하고, A의 실사용 안정성을 기준으로 B의 저위험 개선만 선별한 0.1.5C를 만든다.

## 1. 버전 라벨의 의미

이 문서에서 사용하는 A/B/C는 우선 **브랜치와 검토 단계의 라벨**이다.

- **0.1.5A**: goal 작업 전 상태. 기준 커밋은 `cdfa113`이며 제품 코드는 `V0.1.5` 태그 `85770c4`와 동일하다. 기존 인게임 사용 이력이 있는 생산 기준선이다.
- **0.1.5B**: 현재 작업 트리. `needfix.md`의 항목을 광범위하게 구현했고 현재 소스 메타데이터는 임시로 `0.1.6`까지 올라가 있다. 빌드·순수 로직 테스트는 통과했지만 인게임 활성화 실패를 한 차례 만들었고 최신 수정본은 인게임 미검증이다.
- **0.1.5C**: A에서 새로 시작해 B의 검증 가능한 저위험 변경만 작은 단위로 다시 적용할 후보.

`Info.json`에 `0.1.5A`, `0.1.5B`, `0.1.5C`처럼 문자 suffix를 바로 넣지 않는다. Unity Mod Manager의 버전 파서 계약을 확인하기 전까지 A/B/C는 브랜치 이름과 문서상의 명칭으로만 사용한다. 실제 배포 버전은 C의 인게임 검증이 끝난 뒤 숫자 버전으로 결정한다.

## 2. 현재 저장소 기준점

- 현재 브랜치: `main`
- 현재 HEAD: `cdfa113` (`핸드오프 드래곤볼 / 고칠거 정리`)
- 원격 기준: `origin/main` 및 태그 `V0.1.5`는 `85770c4`
- `cdfa113`은 문서만 바꾸며 제품 코드는 `85770c4`와 같다.
- 현재 작업 트리는 B 변경으로 dirty 상태이며 수정·삭제·신규 파일이 함께 존재한다.
- A 제품 코드: C# 12파일, 5,090줄
- B 제품 코드: C# 20파일, 4,900줄
- B 테스트 harness: `PlanetGauge.Tests`, 192줄
- B는 줄 수는 줄었지만 타입 `48 → 60`, 메서드 약 `196 → 217`, Harmony 패치 클래스 `21 → 22`로 런타임 상태와 상호작용 지점이 늘었다.

## 3. 다음 채팅에서 먼저 할 브랜치 작업

아래 작업은 **현재 dirty 작업 트리를 잃지 않는 순서**로 진행한다. `git reset --hard`, `git checkout -- .`, 광범위한 파일 삭제를 먼저 실행하지 않는다.

### 3.1 B 보존

1. `git status --short --branch`, `git diff --check`, `git diff --stat`를 기록한다.
2. 현재 dirty 작업 트리를 그대로 유지한 채 `codex/0.1.5b` 브랜치를 만든다.
3. `bin/`, `obj/`, 테스트 출력 DLL, 게임 DLL, Unity DLL이 추적 대상에 들어오지 않았는지 확인한다.
4. 제품 코드, 테스트 소스, 문서 변경만 B 스냅샷으로 커밋한다.
5. B의 커밋 hash와 `dist/PlanetGauge/PlanetGauge.dll` hash를 문서에 기록한다.

B는 실험·감사 기록용으로 보존한다. B 위에서 계속 생산 릴리스를 만들지 않는다.

### 3.2 A 기준 브랜치 생성

1. B가 안전하게 커밋되어 작업 트리가 clean인지 확인한다.
2. `cdfa113`에서 `codex/0.1.5a`를 만든다.
3. A를 빌드해 기존 `0.1.5` 패키지 계약이 유지되는지 확인한다.
4. 가능하면 A의 최소 인게임 기준 동작을 다시 기록한다: 활성화, Perfect/Miss, 사망, Restart, 에디터 복귀.

### 3.3 C 작업 브랜치 생성

1. `codex/0.1.5a`에서 `codex/0.1.5c`를 만든다.
2. B 전체 커밋을 cherry-pick하지 않는다.
3. 변경 하나 또는 같은 불변식을 다루는 작은 묶음 하나만 적용하고 별도 커밋한다.
4. Harmony patch target이나 게임플레이 상태가 바뀌는 커밋마다 인게임 활성화 검증을 먼저 수행한다.

## 4. 0.1.5C의 기본 원칙

1. A의 사용자 동작을 기본 계약으로 삼는다.
2. `needfix.md`는 강제 구현 목록이 아니라 위험 등록부로 사용한다.
3. 실제 재현된 버그, 코드만으로 확정되는 상태 복원 오류, 데이터·활성화 손상 가능성만 우선 수정한다.
4. 이론적인 공존 위험이나 미세 성능 문제는 측정·재현 전에는 코드 변경하지 않는다.
5. 구조 분리와 동작 변경을 같은 커밋에 넣지 않는다.
6. 기존에 작동하는 판정·사망 경로의 source of truth를 실게임 증거 없이 교체하지 않는다.
7. 빌드 성공, 순수 로직 테스트, Harmony patch 적용, 인게임 동작을 서로 다른 검증 층으로 기록한다.
8. 새 추상화는 현재 위험을 격리하거나 현재 호출부 둘 이상을 실제로 단순화할 때만 추가한다.

## 5. C에 우선 포함할 변경

다음 항목은 A에 작은 패치로 다시 적용할 가치가 높다. B 파일을 통째로 복사하지 말고 A와 비교해 최소 구현으로 옮긴다.

### 5.1 확정 상태 복원·경계 오류

- **NF-01**: pause 메뉴에서 Restart/Reset이 호출되어도 세션을 초기화할 수 있도록 세션 경계 gate를 판정 gate와 분리한다.
- **NF-08**: `GaugeRuntime.Reset()`뿐 아니라 관련 depth/token을 한 진입점에서 초기화한다. B의 판정 observer 상태는 가져오지 않는다.
- **NF-09**: 임시 `noFail` 변경 전에 실제 원래 값을 저장하고 Postfix/Finalizer에서 그 값으로 복원한다.
- **NF-11**: `scrPlayer.Die()`가 예외를 던진 뒤 `FailAction()`을 연속 호출하지 않는다. 이미 부분 실행됐는지 알 수 없으므로 중복 사망 진입을 피한다.
- **NF-12**: `CheckPostHoldFail(ulong?)`, `ParseEnum<T>(string,T)` 등 reflection 대상은 이름뿐 아니라 static 여부, 매개변수, 반환형까지 검사한다.
- **NF-15**: effect 객체를 만들기 전에 floor와 `bpm × pitch × speed`의 0·NaN·Infinity를 검사한다.
- **NF-16**: 정수 HUD에서 살아 있는 `0.x` 체력은 최소 `1`, 실제 0 이하는 `0`으로 표시한다.
- **NF-17**: 선택 기능인 아이콘 로딩 실패가 에디터 원본 흐름을 깨지 않게 격리한다.
- **NF-22**: 실제 소비자가 없는 warning command 필드는 제거한다.

### 5.2 국소적으로 가치가 명확한 개선

- **NF-05**: actual effect 생성 중간 실패 시 floor effect list와 Unity component를 함께 롤백한다. warning 실패는 actual effect를 막지 않는다.
- **NF-25**: 11줄짜리 `PlanetGaugeText` 같은 단일 영/한 선택 helper는 유지 가능하다. 새로운 범용 localization framework는 만들지 않는다.
- **NF-27**: 실제 시각 스타일이 바뀐 경우에만 style revision을 증가시킨다.

## 6. C에서 구조만 선별 적용할 변경

### 6.1 LevelEvent 파일 분리

NF-21의 파일 분리는 유지 가치가 높다. A의 1,587줄 `PlanetGaugeLevelEvent.cs`를 책임별 파일로 나누되 **동작을 바꾸지 않는 기계적 이동만 먼저 수행**한다.

권장 파일 경계:

- 이벤트 enum·settings·기존 flat command 타입
- registry와 metadata
- runtime effects
- serialization patches
- editor patches
- ApplyEvent patch

첫 분리 커밋에서는 다음을 하지 않는다.

- command 데이터 모델 변경
- JSON key/O-X 의미 변경
- inspector index 수정
- effect transaction 수정
- localization 문구 변경

분리 후 A와 동일한 빌드 결과를 확인한 다음 의미 변경을 별도 커밋으로 적용한다.

### 6.2 순수 상태 kernel

`GaugeStateKernel` 방향은 좋지만 B 버전을 그대로 가져오지 않는다.

1. A의 `ApplyDelta`, `ApplyForcedRecovery`, `PreviewForcedRecovery`에 대한 characterization matrix를 먼저 작성한다.
2. cap on/off, current가 cap 이상인 경우, no-fail, frozen, 양수·음수·0, NaN 보정 경계를 고정한다.
3. 테스트가 A의 현재 결과를 표현한 뒤 동일한 계산만 kernel로 추출한다.
4. 추출 커밋에서는 판정 hook이나 Die token을 바꾸지 않는다.

판정 delta와 누계 index의 중복 통합(NF-26)은 kernel 추출과 함께 수행할 수 있다.

## 7. C에서 제외할 B 변경

다음 변경은 실제 재현 근거가 없고 회귀 표면이 크므로 C에 가져오지 않는다.

### 7.1 판정 observer 전체

- **NF-02의 B 구현**: `scrMarginTracker.AddHit(HitMargin)`를 별도 Harmony patch로 관찰하는 구조
- `SwitchJudgementObserver`
- `SwitchJudgementObservation`
- tracker identity frame 배열
- `MarginTrackerAddHitPatch`
- 이에 종속된 `scrMarginTracker.AddHit` 필수 API 검사
- `scrPlanet.grade` 관련 사후 회귀 검사

A의 `GetHitMargin()` 계산 방식에서 실제 판정 불일치가 재현될 때까지 기존 동작을 유지한다. 불일치가 재현되면 그 사례와 게임 DLL 호출 흐름을 먼저 기록하고 별도 설계한다.

### 7.2 sequence 기반 Die token

- **NF-10의 B 구현**인 `pendingDieSequence`, `nextSequence`, `latestCompletedSequence`를 가져오지 않는다.
- A의 one-shot bool을 유지하되 새 Switch 시작, 모든 조기 종료, 세션 Reset에서 명시적으로 지우는 최소 수정만 검토한다.

### 7.3 이벤트 command 과모델링

- **NF-20의 B 구현**인 `OptionalValue<T>`를 가져오지 않는다.
- `PlanetGaugeAttributeCommand`, `PlanetGaugeMultiplierCommand`, `PlanetGaugeRecoveryCapCommand` 계층을 가져오지 않는다.
- 현재 생산 경로가 Decode → Apply 하나이므로 A의 flat command와 `ApplyX` flag가 더 직접적이다.
- command 모델을 다시 바꾸려면 실제 세 번째 소비자나 반복 오류가 생긴 뒤 검토한다.

### 7.4 근거 없는 상한과 미세 최적화

- **NF-14**: warning/transition 동시 개수 `32`, warning offset `-3600°` 같은 임의 상한은 제거한다. 실제 프레임 문제나 메모리 증가를 측정한 뒤 제품 제한을 정한다.
- **NF-23 일부**: child count가 바뀔 때만 `SetAsLastSibling()`하는 방식은 가져오지 않는다. 다른 모드가 child count 없이 순서만 바꾸면 복구하지 못한다.
- **NF-24**: 8개 점 배열 제거는 Profiler 근거가 없으므로 우선 적용하지 않는다. 성능보다 검증 표면과 가독성을 우선한다.

## 8. B 구현 의도는 유지하되 다시 설계할 항목

- **NF-03**: cleanup 단계는 각각 끝까지 시도하되 `coreIntegrationFailed`, `cleanupPending`, `metadataRollbackPending`의 3상태 재시도 모델은 가져오지 않는다. 단순한 best-effort cleanup과 명확한 로그부터 구현한다.
- **NF-04**: 늦은 registry 실패 시 내부 `IsEnabled`만 내려 UMM 토글과 어긋나는 B 동작을 그대로 쓰지 않는다. 사용자에게 보이는 상태, metadata lifetime, 재시도 시점을 먼저 정한다.
- **NF-06**: 테스트 기반은 유지하되 현재 `PlanetGauge.Tests.exe`를 그대로 C에 포함할지는 재검토한다.
- **NF-07**: UI 생성 실패 시 부분 객체 정리는 유지하되 `failedOwner`로 같은 장면 전체를 영구 차단하지 않는다. 재시도 조건이나 1회 경고만 설계한다.
- **NF-13**: `cacheSelectedEventIndex`가 같은 타일의 복수 이벤트 occurrence를 정확히 뜻하는지 Create/Copy/Delete/Undo/Redo로 검증한 뒤 적용한다.
- **NF-18**: error meter가 꺼져도 PlanetGauge HUD가 보여야 한다는 제품 의도를 사용자와 확정한 뒤 parent fallback을 적용한다.
- **NF-23 일부**: straight/curved/shield `RectTransform` identity 캐시는 저위험이지만 Profiler 또는 명확한 hot-path 근거와 함께 별도 커밋한다.

## 9. 테스트 프로젝트 처리

현재 `PlanetGauge.Tests.exe`는 사용자 프로그램이 아니라 net48 콘솔형 개발 harness다. 더블클릭하면 테스트 후 즉시 종료하며 게임·Unity UI·실제 Harmony 적용을 검증하지 않는다. 배포물에는 포함되지 않았지만 명칭과 실행 방식이 혼동을 만들었다.

C 초기 단계 권고:

1. 현재 B 전용 테스트 harness는 B 브랜치에 보존한다.
2. C에는 A characterization에 실제 필요한 순수 계산 테스트만 다시 도입한다.
3. 가능하면 표준 `dotnet test` 프로젝트로 전환해 사용자 실행 파일처럼 보이지 않게 한다.
4. 테스트 출력의 게임·Unity·Harmony DLL은 테스트 `bin/`에만 존재해야 하며 source/dist/ZIP에 포함하지 않는다.
5. 다음 항목은 자동 테스트가 증명하지 못한다고 명시한다: Harmony patch 적용, Unity ECall, HUD hierarchy, editor inspector, 실제 JSON 왕복, 타 모드 공존.
6. `scrPlanet.grade` 부재, `___grade` 문자열 금지처럼 한 번의 실책에 과적합된 테스트는 핵심 제품 테스트로 취급하지 않는다.

## 10. C 커밋 권장 순서

각 단계는 이전 단계가 인게임 최소 검증을 통과한 뒤 진행한다.

1. `C0`: A 기준선 재현 및 빌드·패키지 기록
2. `C1`: LevelEvent 기계적 파일 분리만 수행
3. `C2`: 원래 no-fail 값 복원과 exact reflection signature
4. `C3`: pause Restart/Reset 및 통합 세션 reset
5. `C4`: effect 입력 선검증과 actual/warning transaction
6. `C5`: 정수 HUD 0.x, icon 저하, dead field 제거
7. `C6`: A characterization test 추가 후 pure state kernel 추출
8. `C7`: 언어 helper와 style revision gate
9. `C8`: 사용자 의도가 확정되고 인게임 재현된 조건부 UI/editor 수정

한 커밋에서 판정 hook, Die token, lifecycle, event schema를 동시에 바꾸지 않는다.

## 11. 단계별 검증 게이트

### 모든 커밋

- `git diff --check`
- Debug/Release 빌드 경고 0, 오류 0
- `Info.json`과 assembly version 일치
- dist에는 `Info.json`, `PlanetGauge.dll`, `Assets/Gaugeline.png`만 존재
- 빈 catch, 전역 Harmony unpatch, TODO/FIXME, 게임 DLL 패키징 없음

### Harmony 또는 lifecycle 변경 커밋

- 게임 완전 종료 후 새 DLL 적용
- UMM 로그에서 모드 버전과 활성화 성공 확인
- enable → disable → enable 반복
- 에디터 버튼/HUD 중복 없음
- Perfect/Miss가 한 번씩만 반영됨
- 사망이 한 번만 발생함
- pause Restart 후 상태 초기화

### LevelEvent 변경 커밋

- 기존 JSON 로드
- 저장 → 재로드
- O/X 유지
- Copy/Delete/Undo/Redo
- 같은 타일 복수 이벤트
- 플레이 → Restart → 에디터 복귀

### 완료 표현

게임을 실행하지 않은 단계는 반드시 **build verified; in-game verification pending**으로 기록한다. 빌드·순수 테스트 통과를 안정성 완료로 표현하지 않는다.

## 12. 이번 채팅에서의 실책

### 12.1 위험 목록을 구현 목록으로 오해

`needfix.md`는 정적 감사에서 찾은 한계·위험·검증 후보였는데, 이를 27개 모두 수정해야 하는 버그 목록으로 취급했다. 재현 여부와 사용자 영향보다 문서의 문제 제기를 우선했다.

### 12.2 실사용 기준선보다 이론적 정합성을 우선

A에서 실제 문제로 확인되지 않은 `GetHitMargin()` 재계산을 교체했다. 판정 source of truth 개선이라는 명분으로 핵심 Harmony 경계를 바꾸면서도, 변경 전 인게임 불일치 사례를 확보하지 않았다.

### 12.3 설치 DLL IL을 잘못 해석

`SwitchChosen()`의 로컬 판정과 `scrFloor.grade` 기록을 `scrPlanet.grade` 필드로 잘못 판단했다. 존재하지 않는 필드를 필수 API와 Harmony `___grade` 주입 대상으로 만들었고 모드 활성화를 완전히 막았다.

정확한 메타데이터 소유자 확인 없이 IL의 `stfld grade`만 보고 타입을 단정한 것이 직접 원인이다.

### 12.4 검증 강도를 과장

Debug/Release 빌드, analyzer, 순수 로직 테스트, Strict 패키지 검사가 통과했다는 이유로 코드 신뢰도가 높아졌다고 평가했다. 그러나 C# 컴파일러는 Harmony 특수 매개변수 `___field`의 실제 필드 존재를 검증하지 않으며, 데스크톱 CLR에서는 Unity ECall 때문에 실제 `PatchAll` smoke test도 동일하게 실행할 수 없다.

자동 테스트가 모두 통과한 상태에서 인게임 활성화가 실패했으므로 검증 층의 한계를 더 강하게 밝혔어야 했다.

### 12.5 변경 단위가 지나치게 큼

판정 hook, Die identity, lifecycle cleanup, event command 모델, UI failure 처리, 성능 미세 최적화, 파일 분리를 한 작업 트리에 동시에 넣었다. 문제가 생겼을 때 어떤 변경이 원인인지 인게임에서 단계적으로 확인하기 어려워졌다.

### 12.6 추상화의 목적을 사후 정당화

`OptionalValue<T>` command 계층, `SwitchJudgementObserver`, sequence token, cleanup pending 상태는 실제 재현된 문제보다 이론적 완결성을 위해 추가됐다. 타입 안정성이나 예외 복구라는 장점은 있었지만 현재 프로젝트 규모와 실게임 검증 능력에 비해 상태 공간을 더 크게 만들었다.

### 12.7 테스트가 구현에 종속됨

테스트 일부가 사용자 동작을 고정하기보다 B의 새 observer·sequence·command 구조 자체를 검증했다. 이후 `scrPlanet.grade` 부재와 `___grade` 금지 검사를 추가했지만 이는 재발 방지 보조 수단이지 모드가 실제로 켜지는지를 증명하지 못한다.

### 12.8 목표 완료 판단이 성급함

인게임 대기 항목이 11개이고 실제 JSON 왕복도 확인하지 못했는데 goal을 완료로 처리했다. “모든 항목을 추적했다”와 “모든 문제를 해결했다”를 구분하지 못했다.

## 13. 재발 방지를 위한 개선 기준

1. 문제마다 `재현됨 / 코드로 확정 / 조건부 위험 / 개선 아이디어`를 먼저 분류한다.
2. `조건부 위험`과 `개선 아이디어`는 기본적으로 코드 변경하지 않는다.
3. 기존 작동 경로를 교체하려면 변경 전 실패 사례와 변경 후 acceptance test가 모두 있어야 한다.
4. 게임 DLL은 필드 이름뿐 아니라 선언 타입, 접근성, 매개변수, 반환형, 실제 호출 경로를 확인한다.
5. Harmony patch가 추가·변경되면 다른 리팩터링보다 먼저 인게임 활성화만이라도 확인한다.
6. 구조 분리 커밋과 의미 변경 커밋을 분리한다.
7. 하나의 커밋은 하나의 불변식만 바꾼다.
8. 테스트는 구현 타입보다 사용자 관찰 결과와 A의 현재 동작을 고정한다.
9. 자동 테스트가 다루지 못한 Unity/Harmony 영역을 완료 보고에 명시한다.
10. 미세 성능 변경은 Profiler 결과가 있을 때만 수행한다.
11. 새 전역 상태나 새 Harmony 경계가 생기면 얻는 이점과 새 실패 모드를 함께 기록한다.
12. 인게임 검증 전에는 B가 A보다 신뢰성이 높다고 주장하지 않는다.

## 14. 다음 채팅에 전달할 실행 지시문

```text
PlanetGauge 0.1.5C 선별 재구성을 진행해줘.
먼저 저장소 루트의 0.1.5C_PLAN.md를 끝까지 읽고 지침을 따른다.

현재 dirty 작업 트리를 0.1.5B 실험 브랜치로 안전하게 보존하고 커밋한 뒤,
cdfa113에서 0.1.5A 기준 브랜치를 만들고, A에서 0.1.5C 브랜치를 시작한다.
작업 트리를 잃을 수 있는 reset/checkout은 B 보존과 clean 상태 확인 전에는 하지 않는다.

B 전체를 cherry-pick하지 말고 문서의 C 커밋 순서대로 작은 변경만 다시 적용한다.
SwitchJudgementObserver, AddHit 판정 patch, sequence 기반 Die token,
OptionalValue<T> command 계층, 임의의 32개/-3600 상한은 C에 넣지 않는다.

LevelEvent 파일 분리는 동작 변경 없이 먼저 수행하고,
각 Harmony/lifecycle 변경 뒤에는 빌드 성공과 인게임 검증 필요 여부를 분리해 보고한다.
게임을 직접 실행하거나 Mods 폴더를 덮어쓰지는 말고, 사용자가 수행할 최소 검증 절차를 제공한다.
```

## 15. 최종 의사결정

- 생산 기준은 A다.
- B는 삭제 대상이 아니라 실험 기록과 선별 소스다.
- C는 A에 B의 일부를 합치는 방식으로 만든다.
- 현재 B를 그대로 출시하지 않는다.
- 파일 분리와 일부 국소 안전성 수정은 가치가 있다.
- 판정 observer, sequence token, command 과모델링, 근거 없는 성능 제한은 제외한다.
- C의 신뢰도는 변경량이 아니라 작은 커밋과 단계별 인게임 검증으로 확보한다.

## 16. 0.1.5C 구현 진행 기록

작업 브랜치: `codex/0.1.5c`

기준선: `cdfa113` (`main`, 0.1.5A 제품 코드)

주의: 아래 자동 검증은 게임 실행을 대신하지 않는다.

### 완료된 독립 커밋

- `6f39db3`: LevelEvent 1,587줄 파일을 책임별 6개 파일로 기계적으로 분리했다. 분리된 본문을 원본과 줄 단위로 재조립해 일치함을 확인했고 타입 48개, Harmony patch 21개가 유지됐다.
- `6cf07d0`: 임시 `noFail`을 실제 원래 값으로 복원하고 `CheckPostHoldFail(ulong?)`, `ParseEnum<T>(string,T)`의 static/매개변수/반환형을 정확히 검사한다.
- `8e80fbd`: pause 상태의 Restart/ResetCustomLevel에서도 세션을 초기화하고, GaugeRuntime·임시 no-fail depth·Switch 관찰 상태를 한 진입점에서 초기화한다.
- `1756801`: custom effect를 만들기 전에 floor와 `bpm × pitch × speed`, angle offset을 검증한다. actual effect 실패는 list/component를 롤백하고 warning 실패는 actual effect를 유지한다.
- `71922b1`: 살아 있는 `0.x` 게이지를 정수 HUD에서 `1`로 표시하고, optional 아이콘 실패가 에디터 원본 로딩을 막지 않게 하며, 실제 소비되지 않는 warning command 필드를 제거한다.
- `b9405dc`: `scrPlayer.Die()` 예외 뒤 `FailAction()`을 연속 호출하지 않고 다음 사망 재진입도 세션 reset 전까지 막는다.
- `63492ea`: 실제 HUD 표현 설정이 달라질 때만 style revision을 증가시킨다.

### 의도적으로 아직 적용하지 않은 항목

- C6 `GaugeStateKernel`: A의 계산 결과를 고정할 표준 테스트 기반과 인게임 C1~C5 게이트가 먼저 필요하다. B의 콘솔형 `PlanetGauge.Tests.exe`와 B 전용 테스트는 가져오지 않았다.
- NF-25 문자열 helper: 지원 언어 정책이 확정되지 않아 새 문자열 정책을 만들지 않았다.
- C8의 UI/editor 조건부 수정: error meter OFF 시 HUD 의도와 editor occurrence 의미를 인게임에서 확인하기 전에는 적용하지 않는다.
- 판정 observer, `scrMarginTracker.AddHit` patch, sequence Die token, `OptionalValue<T>` command 계층, 임의의 warning 상한은 계획대로 제외했다.

### 다음 인게임 게이트

아래 항목이 통과하기 전에는 C6 이후 핵심 계산 리팩터링을 진행하지 않는다.

1. 게임 완전 종료 후 C DLL을 적용하고 UMM에서 버전 `0.1.5` 로드 및 활성화 성공을 확인한다.
2. enable → disable → enable 후 HUD·에디터 버튼·LevelEvent metadata가 중복되지 않는지 확인한다.
3. Perfect, Early/Late, Miss, Overload가 각각 한 번만 반영되고 사망이 한 번만 발생하는지 확인한다.
4. pause 메뉴에서 Restart 및 ResetCustomLevel을 실행해 게이지, blindfold, pending death, warning 표시가 초기화되는지 확인한다.
5. 실패 방지 ON/OFF에서 Miss 복구 뒤 게임의 `noFail` 값이 원래 상태로 돌아오는지 확인한다.
6. `SetPlanetGauge` 기존 JSON 로드 → 저장 → 재로드, O/X, Copy/Delete/Undo/Redo, 같은 타일 복수 이벤트를 확인한다.
7. ForceRecovery actual effect와 음수 warning offset을 실행하고 Restart·에디터 복귀 후 잔여 warning/component가 없는지 확인한다.
8. 정수 표시에서 `0 < gauge < 0.5`가 `1`, 실제 0이 `0`으로 표시되는지 확인한다.

현재 판정: **build verified; in-game verification pending**.

